import type { NextPage } from "next";
import styles from "./styles.module.scss";

const <%= props.component_name %>: NextPage = (): JSX.Element => {
  return <div></div>;
};

export default <%= props.component_name %>;
