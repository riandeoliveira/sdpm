export const giveGreetings = async (req, res) => {
  try {
    res.status(200).send({
      greetings: "Hello, World! Have a nice coding, <%= props.author %> ;)",
    });
  } catch (error) {
    console.log(error);

    res.status(404).send({ greetings: "I can't say hello :(", error });
  }
};
